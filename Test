var el = document.querySelector(
    "[data-foundation-validation~='barclaysuscb.extendedcontentfragmentlist']"
);

var validation = Granite.$(el).adaptTo("foundation-validation");

validation.checkValidity();
validation.updateUI();
