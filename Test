var automaticModel = dialog.find("[name='./modelPath']");
var manualModel = dialog.find("[name='./manualModelPath']");
var curatedModel = dialog.find("[name='./curatedModelPath']");

if (mode === "manual" && manualModel.val()) {
    automaticModel.val(manualModel.val()).trigger("foundation-field-change");
} else if (mode === "curated" && curatedModel.val()) {
    automaticModel.val(curatedModel.val()).trigger("foundation-field-change");
}
