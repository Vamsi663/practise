ModelFactory modelFactory = context.getService(ModelFactory.class);
Resource superTypeResource = context.resourceResolver().getResource("/apps/" + SUPER_TYPE);
try {
    Object result = modelFactory.createModel(superTypeResource, Carousel.class);
    System.out.println("Created: " + result);
} catch (Exception e) {
    e.printStackTrace();
}
