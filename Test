// PAGE EDITOR - intercept BEFORE dialog opens
$(document).on("click", ".js-editor-PageInfo-closePopover", function () {
    setTimeout(function () {
        if (!Granite.author || !Granite.author.ContentFrame) return;
        var pagePath = Granite.author.ContentFrame.getContentPath();
        if (!pagePath) return;

        $.getJSON(pagePath + "/jcr:content.json").done(function (data) {
            if (data["jcr:lockOwner"]) {
                // Close any open dialogs
                document.querySelectorAll("coral-dialog[open]")
                    .forEach(function(d) { d.hide(); });

                // Show error instead
                showLockAlert(data["jcr:lockOwner"]);
            }
            // If not locked - do nothing, dialog opens normally
        });
    }, 500);
});
// Check what element is clicked when you click "Start Workflow" 
// in the Page Info menu
$(document).on("click", function(e) {
    console.log("clicked:", e.target.className, e.target.tagName);
});


