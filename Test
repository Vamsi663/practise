(function ($, Granite) {
    "use strict";

    function disableWorkflowIfLocked() {
        var pagePath = Granite.author.ContentFrame.getContentPath();

        $.getJSON(pagePath + "/jcr:content.json").done(function (data) {
            if (data["jcr:lockOwner"]) {
                $("[class*='WorkflowStart']")
                    .closest("coral-button, button")
                    .attr("disabled", "disabled")
                    .addClass("is-disabled");
            }
        });
    }

    $(document).on("click", "[class*='PageInfo']", function () {
        setTimeout(disableWorkflowIfLocked, 300);
    });

// // SITES CONSOLE
    $(document).on("click", function () {
        setTimeout(function () {
            var $wf = $("a.cq-siteadmin-admin-createworkflow");
            if ($wf.length === 0) return;

            var selectedPaths = [];
            $(".foundation-selections-item").each(function () {
                selectedPaths.push(
                    $(this).data("foundationCollectionItemId") ||
                    $(this).data("path")
                );
            });

            if (selectedPaths.length === 0) return;

            var checks = selectedPaths.map(function (path) {
                return $.getJSON(path + "/jcr:content.json").then(function (data) {
                    if (data["jcr:lockOwner"]) {
                        $wf.css({
                            "pointer-events": "none",
                            "opacity": "0.4",
                            "cursor": "not-allowed"
                        }).attr("title",
                            "Page locked by: " + data["jcr:lockOwner"] +
                            ". Please unlock before starting a workflow."
                        );
                    }
                });
            });
        }, 500);
    });
})(Granite.$, Granite);



// Check what path the JS is actually picking up
$(".foundation-selections-item").each(function() {
    console.log("ItemId:", $(this).data("foundationCollectionItemId"));
    console.log("Path:", $(this).data("path"));
});
