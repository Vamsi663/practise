// Find all buttons in the dialog
$("coral-dialog button, .coral-Dialog button").each(function() {
    console.log("Button text:", $(this).text().trim(), "Class:", $(this).className);
});

// Check the Start Workflow button specifically
$(".cq-dialog-submit").length
$(".cq-dialog-submit").text()

$("[coral-close], .cq-dialog-cancel").length
$("[coral-close], .cq-dialog-cancel").text()

