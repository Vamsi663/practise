$(document).on("coral-overlay:open", function () {
    console.log("Overlay opened");
    console.log("WF element:", $("a.cq-siteadmin-admin-createworkflow").length);
    console.log("Cache:", JSON.stringify(window._lockCache));
});
